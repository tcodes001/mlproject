option_settings:
  "aws:elasticbeanstalk:container:python":
    WSGIPath: application:application

container_commands:
  01_clean_tmp:
    command: "rm -rf /var/tmp/*"
    ignoreErrors: true

  02_set_tmpdir:
    command: "echo 'export TMPDIR=/var/tmp' >> /etc/profile.d/tmpdir.sh"
